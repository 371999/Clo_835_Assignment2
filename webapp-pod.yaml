apiVersion: v1
  kind: Pod
  metadata:
    name: webapp
    labels:
      app: employees
  spec:
    containers:
      - name: webapp
        image: 835512929652.dkr.ecr.us-east-1.amazonaws.com/webapp-image-repo:latest
        ports:
          - containerPort: 8080
